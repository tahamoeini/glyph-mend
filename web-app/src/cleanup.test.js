import {describe,expect,it} from 'vitest';
import {cleanupDocument,documentMetrics,joinPageParagraphs,normalizeText,parsePageRange,plainText,removeRunningMatter} from './cleanup.js';
describe('page ranges',()=>{it('parses, deduplicates, and sorts',()=>expect(parsePageRange('3, 1-2, 2',4)).toEqual([1,2,3]));it('rejects invalid bounds',()=>expect(()=>parsePageRange('0-3',4)).toThrow());});
describe('cleanup',()=>{it('normalizes ligatures and deterministic comma artifacts',()=>expect(normalizeText('ofﬁce‚ tools')).toBe('office, tools'));it('removes repeated edge matter but preserves body',()=>{const pages=[1,2,3].map(page=>({page,text:`BOOK TITLE ${page}\nBody ${page}\n${page}`}));expect(removeRunningMatter(pages).map(p=>p.text)).toEqual(['Body 1','Body 2','Body 3']);});it('joins a lowercase continuation across a page marker',()=>{const value='This is a sufficiently long sentence which continues\n\n<!-- page: 2 -->\n\nonto the next source page';expect(joinPageParagraphs(value)).toContain('continues <!-- page: 2 --> onto');});it('keeps page provenance when requested',()=>expect(cleanupDocument([{page:2,text:'Hello'}],{preserveMarkers:true})).toContain('<!-- page: 2 -->'));});
it('applies browser semantic switches',()=>{const source='☑ Done\n\n$$\nx=1\n$$\n\n[VISUAL_PLACEHOLDER page=1]';const result=cleanupDocument([{page:1,text:source}],{taskLists:true,extractEquations:false,placeholders:false});expect(result).toBe('- [x] Done');});
describe('exports',()=>{it('strips common Markdown for plain text',()=>expect(plainText('# Title\n\n**Bold**')).toBe('Title\n\nBold'));it('reports document features',()=>expect(documentMetrics('# One\n\n$$\nx=1\n$$').equations).toBe(1));});
