name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - name: Compile Python sources
        run: python -m compileall -q src tests
      - run: ruff check src tests

  test:
    name: Test (${{ matrix.os }}, Python ${{ matrix.python }})
    needs: lint
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ matrix.python }}
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - run: python -m pip check
      - run: python -m pytest
      - name: Smoke-test installed commands
        shell: bash
        run: |
          glyphmend --help
          glyphmend gui --help
          python -m glyphmend --help
          md-to-docx --help
          pdf-sanitizer --help
          python -c "import glyphmend, pdf_sanitizer; print(glyphmend.__version__); assert glyphmend.__version__ == pdf_sanitizer.__version__"

  package:
    name: Build package
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: pyproject.toml
      - run: python -m pip install --upgrade pip build
      - run: python -m build
      - name: Install built wheel and smoke-test it
        shell: bash
        run: |
          python -m pip install --force-reinstall dist/*.whl
          python -m pip check
          glyphmend --help
          glyphmend gui --help
          python -m glyphmend --help
          md-to-docx --help
          pdf-sanitizer --help
          python -c "import glyphmend, pdf_sanitizer; print(glyphmend.__version__); assert glyphmend.__version__ == pdf_sanitizer.__version__"
      - uses: actions/upload-artifact@v7
        with:
          name: glyphmend-python
          path: dist/*
          if-no-files-found: error
          retention-days: 14
