{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "glyphmend-companion",
  "description": "Minimal companion commands for the bundled GlyphMend UI.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
